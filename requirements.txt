# Pinned: stable, rarely conflict with Colab's preinstalled stack
Pillow==10.4.0
SQLAlchemy==2.0.0
chromadb==0.5.0
fastapi==0.111.0
google-generativeai==0.7.0
groq>=1.5.0
loguru==0.7.0
nest-asyncio==1.6.0
paddleocr>=2.8.1
paddlepaddle==2.6.2
pandas==2.2.0
pdf2image==1.17.0
pdfplumber==0.11.0
pydantic==2.8.0
pyngrok==7.2.0
python-dotenv==1.0.0
python-magic==0.4.27
python-multipart==0.0.9
rank-bm25==0.2.2
redis==5.0.0
prometheus-client==0.20.0
tiktoken==0.7.0
uvicorn==0.30.0

# Unpinned on purpose: these fight with whatever transformers Colab already
# has preinstalled. Letting pip resolve compatible versions together avoids
# the ImportErrors from hard version pins.
sentence-transformers
transformers
accelerate

# Pinned deliberately: huggingface_hub>=0.26 removed HfFolder, which
# gradio's oauth.py still imports. Keeping this below 0.26 avoids that
# ImportError without needing to upgrade gradio/fastapi together.
huggingface_hub==0.25.2
gradio==4.44.1
